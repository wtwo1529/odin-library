export default class Book {
  constructor(author, title, pages, status, score) {
    this.author = author;
    this.title = title;
    this.pages = pages;
    this.status = status;
    this.score = score;
  }
}
